Your task is to **design and generate a new skill from scratch**. The resulting skill must be modular, highly cohesive, strictly typed, and capable of seamlessly integrating into any context-aware agent's toolset.

**Naming & Identity Constraint:** A skill is a highly specialized functional process or capability, not an agent persona or actor. Ensure the skill's `name` and identity metadata strictly reflect the *action* or *process* itself using abstract nouns or gerunds (e.g., `rust-memory-profiling`, `svg-optimization`, `threlte-hydration`). Persona-based and agentive naming conventions (ending in -er or -or, e.g., `rust-expert`, `svg-optimizer`, `threlte-hydrator`) are strictly prohibited and must be explicitly renamed if updating an existing target.

**Architect Mandate (Intelligent Retrieval & Construction):** **Crucially, before any domain-specific research, you must strictly ingest the core framework, schema guidelines, and best practices located in the `agentskills/` directory.** Use this knowledge to construct a cutting-edge implementation from scratch.

**Skill Execution Mandate (Context-Aware Operation):** Ensure the generated skill strictly enforces context-aware execution. It must efficiently leverage the existing context without requiring a redundant workspace audit. The logic must focus entirely on precise execution: it must strictly validate inputs, perform its responsibilities with maximum efficiency, remain idempotent where applicable to avoid side-effect leakage, and return predictable, structured outputs. It must fail gracefully with highly descriptive telemetry if the provided context or parameters are invalid.

**Technical Constraints:** Ensure all logic reflects state-of-the-art practices verified against empirical data, modern performance profiling, and current 2026 documentation. The resulting code must be production-ready, heavily documented, highly performant, and safe, strictly adhering to the SOLID, DRY, KISS, SoC, and YAGNI principles while demonstrating mastery of the Law of Demeter. Translate these broad software principles into the exact, strict, and idiomatic paradigms required for the skill's specific domain.